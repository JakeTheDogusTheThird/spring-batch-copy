DROP TABLE geographic_units IF EXISTS;

CREATE TABLE geographic_units (
	id BIGINT GENERATED BY DEFAULT IDENTITY PRIMARY KEY,
	anzsic06 VARCHAR(10) NOT NULL,
    area VARCHAR(10) NOT NULL,
    year VARCHAR(4) NOT NULL,
    geo_count VARCHAR(10) NOT NULL,
    ec_count VARCHAR(10) NOT NULL
);